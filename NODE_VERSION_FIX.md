# 🔧 Решение за Metro Bundler Проблем

## Проблем
Metro bundler не може да стартира поради несъвместимост между Node.js 24.4.1 и React Native 0.80.

Грешка: `Error: Cannot find module 'metro-cache-key'`

## Причина
Node.js 24 има известен бъг с module resolution който прави Metro неработещ.

## Решение

### Опция 1: Downgrade на Node.js (Препоръчително)

1. Инсталирайте NVM (Node Version Manager):
```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
```

2. Рестартирайте терминала и инсталирайте Node.js 20:
```bash
nvm install 20
nvm use 20
```

3. Преинсталирайте зависимостите:
```bash
cd /Users/nikolovp/Documents/FinTrack1
rm -rf node_modules yarn.lock
yarn install
```

4. Стартирайте приложението:
```bash
# Терминал 1 - Metro Bundler
yarn start

# Терминал 2 - iOS приложение
yarn ios
```

### Опция 2: Използване на Xcode (Временно решение)

1. Отворете проекта в Xcode:
```bash
open ios/FinTrackNew.xcworkspace  # (Проектът ще се казва FinTrack в UI)
```

2. Изберете симулатор (iPhone 16)

3. Натиснете ▶️ (Run) бутона

4. Xcode автоматично ще стартира Metro с правилните настройки

### Опция 3: Ръчно стартиране (Ако Опция 1 и 2 не работят)

1. Стартирайте приложението без packager:
```bash
yarn react-native run-ios --simulator="iPhone 16" --no-packager
```

2. След като приложението стартира, reload-нете го от симулатора:
   - Натиснете `Cmd + D`
   - Изберете "Reload"

## Проверка

След стартиране на Metro, трябва да видите:
```
 ▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄
 █ ▄▄▄▄▄ █▀▄ ▀▄▀█ ▄▄▄▄▄ █
 █ █   █ █ ▀▄  ▄█ █   █ █
 █ █▄▄▄█ █▄ ▄█▀▄█ █▄▄▄█ █
 █▄▄▄▄▄▄▄█ █ █ █▄█▄▄▄▄▄▄▄█

 Welcome to Metro!
```

## Допълнителна Информация

- React Native 0.80 изисква Node.js 18 или 20
- Node.js 24 все още не е напълно поддържан
- Този проблем е документиран в React Native GitHub issues

## Контакт

Ако проблемът продължава, моля проверете:
1. Node версия: `node --version` (трябва да е v18.x или v20.x)
2. Metro е инсталиран: `ls node_modules/metro`
3. metro-cache-key е инсталиран: `ls node_modules/metro-cache-key`

