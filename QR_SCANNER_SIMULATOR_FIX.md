# 🧪 QR Scanner Debug Mode за Симулатор

## ❓ ЗАЩО НЕ РАБОТИ В СИМУЛАТОРА?

### Проблемът
Когато натиснеш "Започни сканиране" в iOS симулатора или Android емулатора, **не става нищо** защото:

**Симулаторите нямат физическа камера!** 📱❌

- iOS Simulator използва виртуална камера (ако има)
- Android Emulator също няма реална камера
- `react-native-camera-kit` не може да стартира камерата без хардуер

---

## ✅ РЕШЕНИЕ: Debug Mode

Добавих **автоматичен Debug Mode** който:
- Детектира дали приложението работи в симулатор
- Показва специално съобщение
- Симулира сканиране на тестова бележка
- Работи **само в development mode** (`__DEV__`)

---

## 🎯 КАК РАБОТИ DEBUG MODE

### 1. Автоматично Детектиране
```typescript
const IS_SIMULATOR = Platform.OS === 'ios' && !Platform.isPad && Platform.isTVOS === false;
const ENABLE_DEBUG_MODE = __DEV__ && IS_SIMULATOR;
```

### 2. Визуална Индикация
Когато Debug Mode е активен, ще видиш:
- 🧪 Икона в заглавието: "QR Сканер 🧪"
- Променен subtitle: "Debug Mode - Симулирано сканиране"
- Оранжев badge с предупреждение
- Променен текст на бутона: "🧪 Симулирай сканиране"

### 3. Симулирано Сканиране
При натискане на бутона:
1. Показва Alert: "🧪 Debug Mode - Ще симулирам сканиране след 2 секунди"
2. Изчаква 2 секунди (за реалистичност)
3. Автоматично "сканира" тестова бележка:
   ```json
   {
     "store": "Kaufland (TEST)",
     "total": 45.99,
     "items": [
       { "name": "Хляб", "price": 2.50, "quantity": 2 },
       { "name": "Мляко", "price": 3.99, "quantity": 1 },
       { "name": "Кафе", "price": 12.00, "quantity": 1 },
       { "name": "Плодове", "price": 15.00, "quantity": 1 },
       { "name": "Зеленчуци", "price": 12.50, "quantity": 1 }
     ],
     "fiscalNumber": "FN123456789",
     "paymentMethod": "Карта"
   }
   ```
4. Показва резултата като при реално сканиране
5. Можеш да запазиш като транзакция

---

## 🧪 КАК ДА ТЕСТВАШ В СИМУЛАТОРА

### Стъпка 1: Стартирай в симулатор
```bash
# iOS
npx react-native run-ios

# Android
npx react-native run-android
```

### Стъпка 2: Отвори QR Scanner
- Навигирай до средния таб (QR Scanner икона)
- Ще видиш 🧪 Debug Mode индикация

### Стъпка 3: Симулирай сканиране
- Натисни "🧪 Симулирай сканиране"
- Ще видиш Alert с информация
- След 2 секунди ще се покаже резултат

### Стъпка 4: Запази транзакция
- Прегледай симулираните данни
- Натисни "Запази като транзакция"
- Провери в Transactions таба

---

## 📱 РЕАЛНО ТЕСТВАНЕ (Препоръчително)

Debug Mode е **само за development**. За реално тестване:

### iOS - Реално Устройство
```bash
# Свържи iPhone с кабел
# Отвори Xcode и избери устройството
npx react-native run-ios --device
```

### Android - Реално Устройство
```bash
# Свържи Android телефон с кабел
# Активирай USB Debugging в Developer Options
npx react-native run-android
```

### Какво ще работи на реално устройство:
- ✅ Реална камера
- ✅ Реално сканиране на QR кодове
- ✅ Автоматично разпознаване
- ✅ Без Debug Mode индикации
- ✅ Production-ready функционалност

---

## 🔍 РАЗЛИКИ: Debug Mode vs Реално Устройство

| Функция | Debug Mode (Симулатор) | Реално Устройство |
|---------|------------------------|-------------------|
| Камера | ❌ Симулирана | ✅ Реална |
| QR Сканиране | 🧪 Тестови данни | ✅ Реални бележки |
| UI Индикация | 🧪 Debug badge | ✅ Нормален UI |
| Бутон текст | "Симулирай сканиране" | "Започни сканиране" |
| Timeout | ❌ Няма | ✅ 30 секунди |
| Permissions | ❌ Не се изискват | ✅ Camera permission |
| Production Ready | ❌ Само за тестване | ✅ Напълно готово |

---

## 🎨 ВИЗУАЛНИ ПРОМЕНИ В DEBUG MODE

### Header
```
Преди: QR Сканер
След:  QR Сканер 🧪

Преди: Сканиране на касови бележки
След:  Debug Mode - Симулирано сканиране
```

### Welcome Screen
```
Преди: Сканирайте QR кода на вашата касова бележка...
След:  🧪 Debug Mode: Симулаторът няма камера. При натискане 
       на бутона ще се симулира сканиране на тестова бележка.
```

### Бутон
```
Преди: Започни сканиране
След:  🧪 Симулирай сканиране
```

### Debug Badge (Нов)
```
⚠️ Debug Mode: За реално тестване използвай физическо устройство
```

---

## 📝 CONSOLE ЛОГОВЕ

### Debug Mode
```
🧪 DEBUG MODE: Активиран - симулиране на сканиране след 2 секунди
🧪 DEBUG MODE: Симулиране на QR сканиране
📱 QR Scanner: Scanned data: {"store":"Kaufland (TEST)"...
✅ QR Scanner: Successfully parsed receipt data
```

### Реално Устройство
```
📱 QR Scanner: Started scanning with 30s timeout
📱 QR Scanner: Scanned data: {"store":"Kaufland"...
✅ QR Scanner: Successfully parsed receipt data
```

---

## ⚙️ ТЕХНИЧЕСКИ ДЕТАЙЛИ

### Детектиране на Симулатор
```typescript
// Проверява дали е iOS симулатор
const IS_SIMULATOR = Platform.OS === 'ios' && 
                     !Platform.isPad && 
                     Platform.isTVOS === false;

// Активира Debug Mode само в development
const ENABLE_DEBUG_MODE = __DEV__ && IS_SIMULATOR;
```

### Симулирана Функция
```typescript
const simulateScan = useCallback(() => {
  console.log('🧪 DEBUG MODE: Симулиране на QR сканиране');
  
  const mockQRData = JSON.stringify({
    store: "Kaufland (TEST)",
    // ... тестови данни
  });
  
  const mockEvent = {
    nativeEvent: { codeStringValue: mockQRData }
  };
  
  setTimeout(() => {
    handleQRCodeScanned(mockEvent);
  }, 2000);
}, [handleQRCodeScanned]);
```

---

## 🚀 PRODUCTION BUILD

### Debug Mode се деактивира автоматично в production:
```typescript
const ENABLE_DEBUG_MODE = __DEV__ && IS_SIMULATOR;
//                        ^^^^^^
//                        false в production build
```

### Production характеристики:
- ✅ Няма Debug Mode индикации
- ✅ Няма симулирани данни
- ✅ Само реално сканиране
- ✅ Нормален UI
- ✅ Оптимизиран код

---

## 🐛 TROUBLESHOOTING

### Проблем: Debug Mode не се показва
**Причина:** Може да не си в симулатор или не си в dev mode

**Решение:**
1. Провери дали Metro bundler работи
2. Провери дали `__DEV__` е true
3. Restart на Metro: `npx react-native start --reset-cache`

### Проблем: Симулираното сканиране не работи
**Причина:** Може да има грешка в парсирането

**Решение:**
1. Провери конзолата за грешки
2. Провери дали `handleQRCodeScanned` работи
3. Restart на приложението

### Проблем: Искам да тествам на реално устройство
**Решение:**
```bash
# iOS
npx react-native run-ios --device

# Android  
npx react-native run-android
# (с телефон свързан по USB)
```

---

## 📚 СВЪРЗАНИ ФАЙЛОВЕ

- `src/screens/ScannerScreen.tsx` - Главният файл с Debug Mode логика
- `QR_SCANNER_TESTING.md` - Пълно тестване ръководство
- `QR_SCANNER_SUMMARY.md` - Обобщение на функционалността
- `.cursorrules` - QR Scanner patterns и best practices

---

## ✨ ЗАКЛЮЧЕНИЕ

Debug Mode позволява:
- ✅ Тестване на QR Scanner в симулатор
- ✅ Разработка без физическо устройство
- ✅ Бърза итерация и debugging
- ✅ Автоматично се деактивира в production

За **пълно тестване** обаче, използвай **реално устройство**! 📱

---

**Последна актуализация:** 4 Декември 2024  
**Версия:** 1.0.0  
**Статус:** ✅ Debug Mode активен в симулатор







